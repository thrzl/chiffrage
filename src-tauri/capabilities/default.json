{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "new-key", "authenticate", "produce-age"],
  "permissions": [
    "core:default",
    "opener:default",
    "core:webview:allow-create-webview-window",
    "stronghold:default",
    "core:event:allow-emit",
    "dialog:default",
    "store:default",
    "dialog:allow-open"
  ]
}
